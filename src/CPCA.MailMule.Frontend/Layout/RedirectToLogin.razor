@inject NavigationManager Navigation
@inject HttpClient Http

@code {
    protected override void OnInitialized()
    {
        Navigation.NavigateTo($"{Http.BaseAddress}signin", forceLoad: true);
    }
}
